pipeline {
    agent any

    stages {

        stage('Checkout Code') {
            steps {
                git 'https://github.com/riju27/JenkinsloginPage.git'
            }
        }

        stage('Verify Files') {
            steps {
                bat 'dir'
            }
        }
    }

    post {
        success {
            publishHTML(target: [
                reportDir: '.',
                reportFiles: 'index.html',
                reportName: 'Web App',
                keepAll: true,
                alwaysLinkToLastBuild: true
            ])
        }
    }
}
